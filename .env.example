# Copy to .env for LOCAL testing only (.env is gitignored — never commit your key).
# In production this goes in: Netlify → Site settings → Environment variables.
# Get a free key at https://www.football-data.org/client/register
FOOTBALL_DATA_KEY=your_free_token_here
